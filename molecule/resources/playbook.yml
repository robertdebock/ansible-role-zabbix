---
- name: Converge
  hosts: all
  become: yes
  gather_facts: yes

  vars:
    zabbix_server: present
    zabbix_web: present

  roles:
    - ansible-role-httpd
    - ansible-role-mysql
    - ansible-role-zabbix
